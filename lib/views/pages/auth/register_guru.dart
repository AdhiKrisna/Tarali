
import 'package:flutter/material.dart';

class RegisterGuru extends StatelessWidget {
  const RegisterGuru({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Register Guru'),
    );
  }
}