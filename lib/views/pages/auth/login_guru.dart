import 'package:flutter/material.dart';

class LoginGuru extends StatelessWidget {
  const LoginGuru({super.key});

   @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Login Guru'),
    );
  }
}